pub fn transpose_matrix(matrix: &Vec<Vec<i32>>) -> Vec<Vec<i32>> {
    let row_len = matrix.len();
    let col_len = matrix[0].len();
    let mut transposed_matrix = vec![vec![i32::default(); row_len]; col_len];
    todo!()
}
